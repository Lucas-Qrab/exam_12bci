{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Description de la ville</h1>
    <div class="row justify-content-center">
        <div class="col-sm-3">
            <div class="card">
                <div class="card-body">
                    <p class="card-text">
                        ville de : {{ ville.name }}
                    </p>
                </div>
            </div>
        </div>
    </div>

    <h2 class="text-center">Liste des restaurants de la ville :</h2>

    {% set restaurants = ville.restaurants %}
    {% include("restaurant/displayresto.html.twig") %}
{% endblock %}


